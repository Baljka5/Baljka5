import React from "react";
import css from "./module.style.css";
const ToolBar = () => (
  <header className={css.ToolBar}>
    <div>...</div>
    <div className={css.Content}>Logo</div>
    <nav>Menu</nav>
  </header>
);

export default ToolBar;
